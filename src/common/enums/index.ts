export enum SYS_ROLE {
  user,
  admin,
}

export enum SYS_GENDER {
  male,
  female,
}

export enum SYS_PROVIDER {
  system,
  google,
}

export enum SYS_REACTIONS {
  like,
  love,
  wow,
  sad,
  angry,
}

export enum ON_MODEL {
  Post = "Post",
  Comment = "Comment",
}

export enum SYS_USER_RELATION {
  son = 0,
  sister = 1,
  brother = 2,
  father = 3,
  mother = 4,
}
